/**
 * Creates a cell object with x and y properties.
 * @param {number} x - The x coordinate of the cell.
 * @param {number} y - The y coordinate of the cell.
 * @returns {Object} A cell object with x and y properties.
 */
const cell = (x: number, y: number) => {
  return { x, y };
};

export default cell;
